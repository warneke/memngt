package edu.berkeley.icsi.memngt.rpc;

/**
 * Base interface every RPC protocol must inherit. At the moment this interface acts purely as a marker interface. In
 * later versions, it might be used to deal with different RPC protocol versions.
 * 
 * @author warneke
 */
public interface RPCProtocol {
}
